# URL do backend Java (server-side apenas — o navegador nunca acessa
# diretamente, ver src/lib/env e lib/http/api-client.ts)
METAS_API_URL=http://localhost:8080

# Nome e segurança do cookie de sessão
SESSION_COOKIE_NAME=metas_session
SESSION_COOKIE_SECURE=false
